# Set each value to the exact HTTP Authorization header for its credentialEnv.
# Keep this file outside source control and readable only by the relay service.
TEAMS_RELAY_AGENT_A=Bearer REPLACE_WITH_AGENT_CREDENTIAL
