# Candidate receipt: 3eee987

- Issue: `3eee987` — refresh the Local Network MVP dispatch checkpoint.
- Candidate tree: uncommitted worktree at `origin/main=a71615a17e565be9b8eab837e6ee0896a000b118`.
- Changed paths: the two goal documents plus this unit's evidence files under
  `docs/evidence/3eee987-dispatch-20260913/`.
- Scope: one re-entrant scheduling pointer and its user-path snapshot. No runtime, network, provider,
  UI implementation, dependency, lockfile, generated artifact, or second goal/task graph was added.
- Recorded facts: U0 `159b78b` is closed; runtime unit `3742b9a` is queued; L1 `fdec042` is waiting for
  the runtime lifecycle API and retains its red test; C1 `776fcad` remains active in its owned worktree.
- Config boundary: `config.toml` is user intent; `internal.toml` is runtime-owned non-user state; child
  projections are derived; endpoint receive/provide declarations and provider admission ownership are explicit.
